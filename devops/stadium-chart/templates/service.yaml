apiVersion: v1
kind: Service
metadata:
  name: {{ include "stadium-chart.fullname" . }}
  labels:
    {{- include "stadium-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stadium-chart.selectorLabels" . | nindent 4 }}
