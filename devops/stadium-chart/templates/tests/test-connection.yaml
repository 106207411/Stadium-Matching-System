apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stadium-chart.fullname" . }}-test-connection"
  labels:
    {{- include "stadium-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stadium-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
